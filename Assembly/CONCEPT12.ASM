;SEGMENTS ARE ALIGNED WITH THE PARAGRAPH BOUNDARY
;FIRST STACK THEN DATA AND THEN CODE
;THIS ONE IN COM FORMAT
TITLE CONCEPT

CODESG SEGMENT PARA 'CODE'
ASSUME CS:CODESG,DS:CODESG,SS:CODESG
MAIN:
JMP START
ORG 100H

MA DB 'MADHUR'
ITEMA DB 129D
DATE DB 'WHAT IS THE DATE$'
START:
MOV AX,OFFSET MA
;MOV AL,ITEMA  WHY THIS WORKS IN CONCEPT.ASM AND NOT IN CONCEPT11.ASM
MOV DX,OFFSET  ITEMA
MOV AX,23
MOV BX,45
MOV AH,09H
LEA DX,DATE
INT 21H
MOV AX,4C00H
INT 21H
CODESG ENDS
END MAIN